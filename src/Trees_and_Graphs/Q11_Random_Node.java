package Trees_and_Graphs;

public class Q11_Random_Node {
	// solutions 6 created in the TreeNode.java file -> getRandomNode
	// option 7 just went through it -> getRandomNode2 & getIthNode
	//	basically don't re-call the random, just use the previous random value, since we know that ANY x < root can be left out
	//	from the numbers and this is exactly the case of going RIGHT, and vice-versa (in the getIthNode
}
